# web-ads/ is intentionally tracked — post templates for community outreach

# .gitkeep files are local only (used for VS Code folder visibility)
**/.gitkeep

# Local strategy notes — not for repo
_notes/
